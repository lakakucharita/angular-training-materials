import { FriendlyAnswerCountPipe } from './friendly-answer-count.pipe';

describe('FriendlyAnswerCountPipe', () => {
  it('create an instance', () => {
    const pipe = new FriendlyAnswerCountPipe();
    expect(pipe).toBeTruthy();
  });
});
